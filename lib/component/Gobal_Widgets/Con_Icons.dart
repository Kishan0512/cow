import 'package:flutter/material.dart';

import 'Con_Color.dart';

const Own_ArrowBack = Icon(Icons.arrow_back_ios_new);
const Own_Refresh = Icon(Icons.sync_outlined);
const Own_Search = Icon(Icons.search_sharp);
const Own_Search_White = Icon(Icons.search_sharp, color: whiteColor);
const Own_Share = Icon(Icons.share);
const Own_close_eye = Icon(Icons.visibility_off_sharp);
const Own_open_eye = Icon(Icons.remove_red_eye);
const Own_menu = Icon(Icons.menu);
const Own_highlight_remove = Icon(Icons.highlight_remove);
const Own_Close = Icon(Icons.close);
const Own_Close_white = Icon(Icons.close, color: whiteColor);
const Own_tune = Icon(Icons.tune_rounded);
const Own_add = Icon(Icons.add);
const Own_bluetooth = Icon(Icons.bluetooth);
const OwnM_number = Icon(Icons.call, size: 15);
const Own_delete = Icon(Icons.delete);
const Own_time = Icon(Icons.access_time_sharp);
const Own_calendar = Icon(Icons.calendar_today_rounded);
